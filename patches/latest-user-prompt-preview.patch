diff --git a/codex-rs/thread-store/src/thread_metadata_sync.rs b/codex-rs/thread-store/src/thread_metadata_sync.rs
index 9a4a679..66a1e34 100644
--- a/codex-rs/thread-store/src/thread_metadata_sync.rs
+++ b/codex-rs/thread-store/src/thread_metadata_sync.rs
@@ -317,10 +317,11 @@ impl ThreadMetadataSync {
                 self.first_user_message_seen = true;
                 update.first_user_message = Some(preview.clone());
             }
-            if !self.preview_seen {
-                self.preview_seen = true;
-                update.preview = Some(preview);
-            }
+            // The resume picker is a re-entry surface. Keep it pointed at the
+            // most recent human ask, while first_user_message remains the
+            // immutable historical anchor for title and provenance.
+            self.preview_seen = true;
+            update.preview = Some(preview);
         }
         if !self.title_seen {
             let title = strip_user_message_prefix(user.message.as_str());
@@ -501,7 +502,7 @@ mod tests {
     }
 
     #[test]
-    fn goal_update_sets_preview_without_overriding_existing_preview() {
+    fn user_message_overrides_goal_preview() {
         let thread_id = ThreadId::new();
         let sync = resume_sync(
             thread_id,
@@ -515,7 +516,7 @@ mod tests {
         );
 
         let update = sync.take_pending_update().expect("pending metadata update");
-        assert_eq!(update.patch.preview.as_deref(), Some("ship the refactor"));
+        assert_eq!(update.patch.preview.as_deref(), Some("first user text"));
         assert_eq!(update.patch.title.as_deref(), Some("first user text"));
         assert_eq!(
             update.patch.first_user_message.as_deref(),
@@ -524,7 +525,7 @@ mod tests {
     }
 
     #[test]
-    fn later_user_messages_do_not_emit_existing_preview_fields() {
+    fn later_user_messages_refresh_the_resume_preview() {
         let thread_id = ThreadId::new();
         let mut sync = resume_sync(
             thread_id,
@@ -541,7 +542,7 @@ mod tests {
             )))])
             .expect("updated_at touch");
 
-        assert_eq!(update.patch.preview, None);
+        assert_eq!(update.patch.preview.as_deref(), Some("later user text"));
         assert_eq!(update.patch.title, None);
         assert_eq!(update.patch.first_user_message, None);
         assert!(update.patch.updated_at.is_some());
diff --git a/codex-rs/thread-store/src/types.rs b/codex-rs/thread-store/src/types.rs
index 192e6be..109fbde 100644
--- a/codex-rs/thread-store/src/types.rs
+++ b/codex-rs/thread-store/src/types.rs
@@ -561,7 +561,7 @@ pub struct StoredThread {
     pub forked_from_id: Option<ThreadId>,
     /// The ID of the parent thread. This will only be set if this thread is a subagent.
     pub parent_thread_id: Option<ThreadId>,
-    /// Best available user-facing preview, usually the first user message.
+    /// Latest non-empty user-facing prompt, used for session discovery and resume.
     pub preview: String,
     /// Optional user-facing thread name/title.
     pub name: Option<String>,
